# This file is part of the standard R package testing structure.
# It tells R CMD check to run the tests in the testthat/ subdirectory.

library(testthat)
library(gwArsenicR)

test_check("gwArsenicR")